name: Xcode - pull request action

on:
  pull_request:

jobs:
  build:
    name: Build and XCTest
    runs-on: self-hosted 

    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build time summary
        run: |
         xcodebuild -project swiftUI_XCUITest.xcodeproj -scheme swiftUI_XCUITest build -destination "id=00008101-001E70A42199001E"         
